// imports
import { ReactNode } from "react"

// layout prop types
export type LayoutPropType = {
    children: ReactNode;
}

// text prop types
export type TextPropType = {
    children: ReactNode;
}

// brand prop types
export type BrandPropType = {
    height?: number;
    width?: number;
}

// sub text prop types
export type SubTextPropType = {
    children: ReactNode;
}